import { Meta } from "@storybook/addon-docs/blocks";
import { Button } from "../../components/ui/button";

export const ButtonExample = ({ variant, size, children, ...props }) => (
  <Button variant={variant} size={size} {...props}>
    {children}
  </Button>
);

export const ButtonGrid = () => (
  <div style={{ display: 'flex', flexWrap: 'wrap', gap: '12px', alignItems: 'center' }}>
    <Button variant="default">기본</Button>
    <Button variant="contained">강조</Button>
    <Button variant="outline">아웃라인</Button>
    <Button variant="textOnly">텍스트</Button>
    <Button variant="buttonText">밑줄 텍스트</Button>
    <Button variant="buttonIconContained" size="buttonIconMedium">🔍</Button>
    <Button variant="buttonIconTextOnly" size="buttonIconMedium">⚙️</Button>
  </div>
);

export const SizeGrid = () => (
  <div style={{ display: 'flex', flexWrap: 'wrap', gap: '12px', alignItems: 'center' }}>
    <Button variant="contained" size="lg">큰 버튼</Button>
    <Button variant="contained" size="md">중간 버튼</Button>
    <Button variant="contained" size="sm">작은 버튼</Button>
  </div>
);

<Meta title="UI Components/Button/Documentation" />

<div className="sb-container">
  <div className='sb-section-title'>
    # Button 컴포넌트 문서

    사용자 인터랙션을 위한 핵심 UI 요소입니다. 다양한 스타일과 크기를 제공하여 모든 디자인 요구사항을 충족합니다.
  </div>

  <div className="sb-section">
    <div className="sb-section-item">
      <h3>🎨 모든 Variant</h3>
      <p>Button 컴포넌트가 제공하는 모든 스타일 변형을 확인해보세요.</p>
      <ButtonGrid />
    </div>
  </div>

  <div className="sb-section">
    <div className="sb-section-item">
      <h3>📏 모든 Size</h3>
      <p>다양한 크기의 버튼을 상황에 맞게 사용할 수 있습니다.</p>
      <SizeGrid />
    </div>
  </div>

  <div className="sb-section">
    <div className="sb-section-item">
      <h3>✨ 주요 특징</h3>
      <ul>
        <li><strong>7가지 variant</strong>: default, contained, outline, textOnly, buttonText, buttonIconContained, buttonIconTextOnly</li>
        <li><strong>6가지 size</strong>: default, lg, md, sm, buttonText, buttonIconLarge, buttonIconMedium</li>
        <li><strong>접근성 우선</strong>: 키보드 네비게이션 및 스크린 리더 지원</li>
        <li><strong>커스터마이징 가능</strong>: Tailwind CSS 클래스로 쉬운 스타일링</li>
        <li><strong>타입 안전성</strong>: TypeScript 완전 지원</li>
      </ul>
    </div>
  </div>

  <div className="sb-section">
    <div className="sb-section-item">
      <h3>🎯 사용 가이드라인</h3>
      
      <h4>Primary Action (주요 액션)</h4>
      <div style={{ marginBottom: '16px' }}>
        <ButtonExample variant="contained">저장하기</ButtonExample>
        <p><strong>contained</strong> variant를 사용하세요. 한 화면에 하나만 사용하는 것을 권장합니다.</p>
        <p><em>예시: 저장, 제출, 확인, 구매하기, 로그인</em></p>
      </div>

      <h4>Secondary Action (보조 액션)</h4>
      <div style={{ marginBottom: '16px' }}>
        <ButtonExample variant="outline">취소</ButtonExample>
        <p><strong>outline</strong> 또는 <strong>default</strong> variant를 사용하세요.</p>
        <p><em>예시: 취소, 뒤로가기, 목록 보기</em></p>
      </div>

      <h4>Minimal Action (최소 액션)</h4>
      <div style={{ marginBottom: '16px' }}>
        <ButtonExample variant="textOnly">더보기</ButtonExample>
        <p><strong>textOnly</strong> variant를 사용하세요. 최소한의 시각적 영향을 원할 때 사용합니다.</p>
        <p><em>예시: 링크, 부가 기능, 더보기</em></p>
      </div>

      <h4>Icon Action (아이콘 액션)</h4>
      <div style={{ marginBottom: '16px' }}>
        <ButtonExample variant="buttonIconContained" size="buttonIconMedium">🔍</ButtonExample>
        <p><strong>buttonIcon</strong> variants를 사용하세요. 아이콘만으로 의미가 명확할 때 사용합니다.</p>
        <p><em>예시: 검색, 설정, 닫기, 메뉴</em></p>
      </div>
    </div>
  </div>

  <div className="sb-section">
    <div className="sb-section-item">
      <h3>♿ 접근성</h3>
      <ul>
        <li><strong>터치 타겟</strong>: 최소 44px × 44px 크기 유지</li>
        <li><strong>명확한 텍스트</strong>: 설명적이고 이해하기 쉬운 라벨 사용</li>
        <li><strong>키보드 접근</strong>: Tab 키로 포커스 이동 가능</li>
        <li><strong>색상 대비</strong>: WCAG 가이드라인 준수</li>
        <li><strong>상태 표시</strong>: disabled, loading 등 상태를 명확히 표현</li>
      </ul>
    </div>
  </div>

  <div className="sb-section">
    <div className="sb-section-item">
      <h3>🔧 사용 예시</h3>
      
      ```tsx
      import { Button } from '@/components/ui/button';

      // 기본 사용법
      <Button>기본 버튼</Button>

      // Variant와 Size 지정
      <Button variant="contained" size="lg">
        큰 강조 버튼
      </Button>

      // 비활성화 상태
      <Button disabled>
        비활성화된 버튼
      </Button>

      // 이벤트 핸들러
      <Button onClick={() => console.log('클릭!')}>
        클릭 버튼
      </Button>

      // 아이콘 버튼
      <Button variant="buttonIconContained" size="buttonIconMedium">
        🔍
      </Button>
      ```
    </div>
  </div>

  <div className="sb-section">
    <div className="sb-section-item">
      <h3>⚠️ 주의사항</h3>
      <ul>
        <li><strong>Primary 버튼 남용 금지</strong>: contained variant는 한 화면에 하나만 사용</li>
        <li><strong>적절한 크기 선택</strong>: 콘텐츠와 맥락에 맞는 크기 사용</li>
        <li><strong>일관성 유지</strong>: 같은 기능의 버튼은 동일한 스타일 사용</li>
        <li><strong>로딩 상태 고려</strong>: 비동기 작업 시 적절한 피드백 제공</li>
      </ul>
    </div>
  </div>
</div>

<style>
  {`
  .sb-container {
    margin-bottom: 48px;
  }

  .sb-section {
    width: 100%;
    display: flex;
    flex-direction: column;
    height: 100%;
  }

  .sb-section-title {
    margin-bottom: 32px;
  }

  .sb-section-item {
    background: #f6f9fc;
    border-radius: 8px;
    padding: 20px;
    margin-bottom: 24px;
  }

  .sb-section-item h3 {
    margin-top: 0;
    color: #1ea7fd;
  }

  .sb-section-item h4 {
    color: #333;
    margin-top: 24px;
    margin-bottom: 8px;
  }

  .sb-section-item p {
    margin: 8px 0;
    line-height: 1.5;
  }

  .sb-section-item ul {
    margin: 8px 0;
    padding-left: 20px;
  }

  .sb-section-item li {
    margin-bottom: 4px;
    line-height: 1.4;
  }

  pre {
    background: #f8f8f8;
    border-radius: 4px;
    padding: 16px;
    overflow-x: auto;
    font-size: 14px;
    line-height: 1.4;
  }
  `}
</style>
