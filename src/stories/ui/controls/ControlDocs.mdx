import { Meta } from '@storybook/addon-docs/blocks';
import { RadioGroup, RadioGroupItem } from '../../../components/ui/radio-group';
import Checkbox from '../../../components/ui/checkbox';
import { Switch } from '../../../components/ui/switch';
import { cn } from '../../../lib/utils';
import { useState } from 'react';

export const RadioGrid = () => {
  const [value, setValue] = useState('option1');

return (

<RadioGroup onValueChange={setValue} value={value}>
  <div className="flex flex-col space-y-3">
    <div className="flex items-center space-x-2">
      <RadioGroupItem value="option1" id="option1" />
      <label htmlFor="option1" className="text-sm font-medium leading-none">
        옵션 1
      </label>
    </div>
    <div className="flex items-center space-x-2">
      <RadioGroupItem value="option2" id="option2" />
      <label htmlFor="option2" className="text-sm font-medium leading-none">
        옵션 2
      </label>
    </div>
    <div className="flex items-center space-x-2">
      <RadioGroupItem value="option3" id="option3" />
      <label htmlFor="option3" className="text-sm font-medium leading-none">
        옵션 3
      </label>
    </div>
  </div>
</RadioGroup>
); };

export const CheckboxGrid = () => {
  return (
    <div className="flex flex-col gap-4">
      <Checkbox label="기본 체크박스" id="checkbox1" />
      <Checkbox label="체크된 상태" checked={true} id="checkbox2" />
      <Checkbox
        label="비활성화된 체크박스"
        checked={true}
        disabled={true}
        id="checkbox3"
      />
    </div>
  );
};

export const ToggleGrid = () => {
  const [toggle1, setToggle1] = useState(false);
  const [toggle2, setToggle2] = useState(true);

return (
<div className="flex flex-col gap-4">
<div className="flex items-center gap-3">
<Switch checked={toggle1} onCheckedChange={setToggle1} />
<span className="font-xxl-1">기본 토글</span>
</div>
<div className="flex items-center gap-3">
<Switch checked={toggle2} onCheckedChange={setToggle2} />
<span className="font-xxl-1">활성화된 토글</span>
</div>
<div className="flex items-center gap-3">
<Switch disabled />
<span className="font-xxl-1 text-gray-400">
비활성화된 토글
</span>
</div>
<div className="flex items-center gap-3">
<Switch checked disabled />
<span className="font-xxl-1 text-gray-400">
활성화되고 비활성화된 토글
</span>
</div>
</div>
);
};

<Meta title="UI Components/Controls/Documentation" />

<div className="flex flex-col gap-4">
  <div>
    <h1>Controls 컴포넌트 문서</h1>
    <p>UI 컴포넌트를 위한 핵심 UI 요소입니다. 다양한 스타일과 크기를 제공하여 모든 디자인 요구사항을 충족합니다.</p>
  </div>

<div className="bg-gray-50 p-4">
  <h3>Radio</h3>
  <p>단일의 항목을 선택하는 Radio 컴포넌트</p>
  <RadioGrid />
</div>

<div className="bg-gray-50 p-4">
  <h3>Checkbox</h3>
  <p>다중 항목을 선택하는 Checkbox 컴포넌트</p>
  <CheckboxGrid />
</div>

<div className="bg-gray-50 p-4">
  <h3>Toggle</h3>
  <p>ON/OFF를 관리하는 Switch 컴포넌트</p>
  <ToggleGrid />
</div>

</div>
