import { Meta } from '@storybook/addon-docs/blocks';

export const ShadowSwatch = ({ name, className, description }) => (
  <div className="flex flex-col items-center p-6">
    <div
      className={`h-24 w-24 rounded-lg border border-gray-200 bg-white ${className}`}
    ></div>
    <div className="mt-4 text-center">
      <h3 className="">{name}</h3>
      <p className="mt-1">{description}</p>
    </div>
  </div>
);

export const ShadowGroup = ({ shadows }) => (
  <div className="mb-8">
    <div className="grid grid-cols-1 gap-8 md:grid-cols-2 lg:grid-cols-3">
      {shadows.map((shadow, index) => (
        <ShadowSwatch key={index} {...shadow} />
      ))}
    </div>
  </div>
);

<Meta title="Design System/Shadows" />

<div>
  <div>
    <h1>그림자 시스템</h1>
    <p>프로젝트에서 사용되는 모든 그림자 스타일들입니다. 일관된 깊이감과 시각적 계층을 제공합니다.</p>
  </div>

  <div className="flex flex-col gap-10">
    <ShadowGroup
      shadows={[
        { 
          name: "shadow-1", 
          className: "shadow-1", 
          description: "기본 그림자 - 카드, 버튼 등에 사용" 
        },
        { 
          name: "shadow-2", 
          className: "shadow-2", 
          description: "상단 그림자 - 하단 고정 요소에 사용" 
        }
      ]}
    />
  </div>
</div>
