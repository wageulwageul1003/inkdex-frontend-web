import { Button } from '@/components/ui/button';

export default function Login() {
  return (
    <div className="flex flex-col pt-5">
      <Button variant="default">둘러보기</Button>
      <Button variant="cta">로그인</Button>
    </div>
  );
}
